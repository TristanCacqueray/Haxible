- name: Adding numbers
  debug:
    msg:
      "Adding {{ x }} + {{ y }}"
  register: add_result

- name: Checking results
  assert:
    that:
      - "x == '42' and y == '21'"
      - "add_result['msg'] == 'Adding 42 + 21'"
